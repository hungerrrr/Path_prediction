diff --git a/Path_prediction_clean/src/streaming_predictor.py b/Path_prediction_clean/src/streaming_predictor.py
--- a/Path_prediction_clean/src/streaming_predictor.py
+++ b/Path_prediction_clean/src/streaming_predictor.py
@@ -2,10 +2,10 @@
 
 from __future__ import annotations
 
-import json
 import math
+import threading
 from collections import deque
-from dataclasses import dataclass, field, asdict
+from dataclasses import asdict, dataclass
 from pathlib import Path
 from typing import Any, Literal
 
@@ -18,12 +18,26 @@ from gru.core import DataConfig
 
 CENTER_POINTS = ("nose", "midhip", "rankle", "lankle")
 CENTER_WEIGHTS = np.asarray([0.3, 0.5, 0.1, 0.1], dtype=np.float32)
+SUPPORTED_METHODS = (
+    "gru",
+    "eigen_transformer",
+    "leapfrog_diffusion",
+)
+STATE_SCHEMA_VERSION = "streaming_predictor_state.v1"
 
 
 # ─── 数据契约 ────────────────────────────────────────
 
 
+class JsonDataclassMixin:
+    """为对外数据契约提供可直接交给 JSON 编码器的字典。"""
+
+    def to_dict(self) -> dict[str, Any]:
+        return asdict(self)
+
+
 @dataclass
-class PersonFrameData:
+class PersonFrameData(JsonDataclassMixin):
@@ -36,7 +50,7 @@ class PersonFrameData:
 
 
 @dataclass
-class FrameData:
+class FrameData(JsonDataclassMixin):
@@ -44,7 +58,7 @@ class FrameData:
 
 
 @dataclass
-class CandidatePath:
+class CandidatePath(JsonDataclassMixin):
@@ -51,7 +65,7 @@ class CandidatePath:
 
 
 @dataclass
-class PredictionResult:
+class PredictionResult(JsonDataclassMixin):
@@ -106,21 +120,43 @@ class StreamingPredictor:
         num_samples: int = 6,
         max_persons: int = 50,
         stale_frame_threshold: int = 30,
+        max_frame_gap: int = 1,
         gru_confidence: float = 0.6,
     ) -> None:
+        if method not in SUPPORTED_METHODS:
+            raise ValueError(f"不支持的模型方法: {method}")
+        if max_persons < 1:
+            raise ValueError("max_persons 必须大于 0")
+        if stale_frame_threshold < 1:
+            raise ValueError("stale_frame_threshold 必须大于 0")
+        if max_frame_gap < 1:
+            raise ValueError("max_frame_gap 必须大于 0")
+        if num_samples < 1:
+            raise ValueError("num_samples 必须大于 0")
+        scale = np.asarray(normalization_scale, dtype=np.float32)
+        if scale.shape != (2,) or not np.all(np.isfinite(scale)):
+            raise ValueError("normalization_scale 必须是两个有限数值")
+        if np.any(scale <= 0):
+            raise ValueError("normalization_scale 必须大于 0")
+
         self.model = model
         self.method = method
         self.config = config
-        self.scale = np.asarray(normalization_scale, dtype=np.float32)
+        self.scale = scale
         self.device = device
         self.num_samples = num_samples
         self.max_persons = max_persons
         self.stale_frame_threshold = stale_frame_threshold
+        self.max_frame_gap = max_frame_gap
         self.gru_confidence = gru_confidence
 
         self._buffers: dict[str, deque] = {}
         self._bboxes: dict[str, np.ndarray] = {}
         self._last_seen: dict[str, int] = {}
+        self._lock = threading.RLock()
